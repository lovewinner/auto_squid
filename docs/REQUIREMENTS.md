# auto_squid — Requirements

目标
- 在计算机 B 上运行的 auto-squid 作为 HTTP 代理入口，接受来自计算机 A 的 HTTP/HTTPS 请求。
- auto-squid 按域名为每次请求选择最优出站代理节点（节点可运行在 B、C、D 等主机），并将客户端请求转发到该代理以访问目标网站。

参与者
- 客户端（计算机 A）：配置为使用 B:10808（或其它 listen 端口）为 HTTP/HTTPS 代理。
- 管理/运维（人）：部署、配置代理列表、监控与故障响应。
- 代理节点（计算机 B/C/D）：运行真实代理（Squid、TinyProxy、或其他转发/递归代理），接受转发请求。

关键用例
1. 请求转发：客户端发起 HTTP/HTTPS 请求 -> auto-squid 根据域名选代理 -> 将请求转发到选中代理 -> 代理代理向目标站点请求并返回结果 -> auto-squid 将响应回传客户端。
2. 健康探测：定期向每个代理执行探测（TCP、短 HTTP GET、可选小下载），记录延迟/吞吐/成功率。
3. 打分与选择：对每个 (domain, proxy) 计算时间加权分数并选出最佳代理；支持冷启动策略与最小样本数阈值。
4. 管理 API：列出/添加/删除/启用/禁用代理，查询域名评分与当前路由决策，查看探测状态。
5. 故障切换：当所选代理失败，尝试下一个最佳代理或返回错误并记录事件。

功能性需求（要点）
- 支持 HTTP 和 HTTPS（CONNECT）代理语义。
- 支持按域名路由决策（包括子域匹配和通配规则）。
- 支持可配置的探测策略（probe_url、interval、timeout、并发、历史窗口、半衰期）。
- 支持持久化/加载代理列表（YAML/JSON）和可选的运行时 API 管理。
- 提供 /health、/metrics、/score、/proxies 等管理接口。
- 防止同步阻塞，探测与转发需要异步并发实现（asyncio 或线程池）。

非功能性需求
- 可用性：探测与路由引导不应显著增加客户端请求延迟（优先使用已缓存或近实时的分数）。
- 可扩展性：支持 100+ 代理实例与并发探测/转发（通过并发限制与批处理）。
- 可观测性：提供日志与基本指标（探测延迟分布、成功率、每域选择率）。
- 安全性：建议管理 API 使用身份验证并仅在受控网络中暴露；敏感凭据不得明文提交到代码仓库。

约束与假设
- 真实的代理节点负责目标站点的外网访问与 TLS 终端（对于 HTTPS 的 CONNECT，auto-squid 仅转发连接到选定代理）。
- 客户端将 B 配置为上游代理，或通过透明网关将流量重定向到 B。
- 初始版本可假设代理使用明文 HTTP 基本通讯；生产环境需使用 TLS/认证。

验收标准
- 在本地网络中，客户端通过 B 成功访问 HTTP/HTTPS 站点，且 auto-squid 按域名选择并使用指定的代理节点完成访问（可观察到代理访问日志）。
- 管理 API 能列出代理、返回探测状态、并展示按域名的推荐代理。
- 能在 1 分钟内完成首次探测并在 60s 内对常见域名产生可用的评分（或标记为 warming/cold）。

测试场景（示例）
- 功能测试：客户端对 http://example.com 发起请求，验证请求经由 B->selected-proxy->example.com 的链路。
- 故障测试：关闭首选代理节点，验证请求自动切换到次选代理。
- 性能测试：以并发 50 并发短连接探测代理，验证探测 loop 无死锁并在可配置并发下工作。